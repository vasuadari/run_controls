alias s:= switch

switch:
  sudo nix run nix-darwin -- switch --flake ~/.run_commands/.config/darwin#

nflake:
  $EDITOR ~/.run_commands/.config/darwin/flake.nix

nhome:
  $EDITOR ~/.run_commands/.config/darwin/home.nix
